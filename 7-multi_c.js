const arg = prompt("Enter a number: ");
const num = parseInt(arg);

if(isNaN(num)) {
    console.log("Missing number of occurences");
    
}else{
    for (let i = 0; i < num; i++) {
        console.log("C is fun");
        
        
    }
}